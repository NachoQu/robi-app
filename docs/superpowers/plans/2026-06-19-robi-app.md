# Robi — App de aprendizaje gamificado · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir el MVP end-to-end del flujo padre→niño: el padre carga una URL de YouTube, una IA genera un quiz, y el niño lo juega con Robi acumulando puntos y badges.

**Architecture:** Next.js 15 (App Router) en Vercel + Supabase (Auth + Postgres con RLS). Toda la lógica sensible (APIs externas) vive en Server Actions. El acceso al LLM está abstraído tras `lib/ai/` para ser intercambiable. La transcripción se obtiene de Supadata. El padre se autentica con Supabase Auth; los perfiles de hijos son filas dentro de su sesión (RLS siempre activo). El PIN es una barrera de UI.

**Tech Stack:** Next.js 16 (App Router), TypeScript, Tailwind CSS, shadcn/ui, Framer Motion, Supabase (Postgres + Auth + RLS), Claude `claude-sonnet-4-6` (via `@anthropic-ai/sdk`), Supadata API, YouTube IFrame Player API, Web Speech API, Vitest (tests de lib).

## Global Constraints

- Modelo LLM por defecto: `claude-sonnet-4-6`. Todo acceso al LLM pasa por la interfaz `AIProvider` en `lib/ai/` — ningún otro módulo importa el SDK directamente.
- Las API keys (`ANTHROPIC_API_KEY`, `SUPADATA_API_KEY`) **solo** se usan en código server-side. Nunca en componentes cliente ni en `NEXT_PUBLIC_*`.
- Toda tabla tiene RLS habilitado, scopeado a `auth.uid()` (directa o vía join).
- Quiz: exactamente **5 preguntas**, cada una con 4 opciones (`options: string[4]`), `correct_index ∈ 0..3`, y `source_quote` (fragmento literal del transcript).
- Scoring: 10 pts base por completar (siempre) + 5 pts por acierto. Mín 10 / máx 35 por video. Siempre 1 badge por completar.
- Límites free tier: 1 perfil de hijo por cuenta; 5 videos por perfil; sin borrado de videos.
- Estética: playful/colorida. Robi presente en loader, quiz y resultado.
- Canje de vales: **mock** — confirmación visual, no escribe `redemptions` ni descuenta puntos.

---

## File Structure

```
robi-app/
├── app/
│   ├── layout.tsx, globals.css, page.tsx                 # home: selección de perfiles
│   ├── (auth)/login/page.tsx, signup/page.tsx
│   ├── onboarding/page.tsx
│   ├── parent/
│   │   ├── layout.tsx                                    # gate por PIN
│   │   ├── page.tsx                                      # dashboard
│   │   ├── add-video/page.tsx
│   │   └── vouchers/page.tsx
│   └── kid/[profileId]/
│       ├── page.tsx                                      # biblioteca
│       ├── watch/[videoId]/page.tsx
│       ├── quiz/[videoId]/page.tsx
│       ├── result/page.tsx
│       ├── album/page.tsx
│       └── rewards/page.tsx
├── lib/
│   ├── ai/index.ts, types.ts, claude-provider.ts, prompts.ts
│   ├── youtube/parse-url.ts, transcript.ts
│   ├── supabase/server.ts, client.ts, middleware.ts, types.ts
│   ├── scoring.ts
│   └── video-pipeline.ts                                 # Server Action de procesamiento
├── components/
│   ├── robi/Robi.tsx                                     # mascota + moods
│   └── ui/...                                            # shadcn
├── supabase/migrations/
│   ├── 0001_schema.sql, 0002_rls.sql, 0003_rpc.sql, 0004_seed_vouchers.sql
├── actions/                                              # server actions agrupadas por dominio
│   ├── auth.ts, profiles.ts, videos.ts, quiz.ts, pin.ts
├── tests/
│   ├── youtube-parse-url.test.ts, scoring.test.ts, ai-anchoring.test.ts, claude-provider.test.ts
├── .env.local.example
└── middleware.ts                                         # refresh de sesión Supabase
```

---

## Phase 0 — Setup del proyecto

### Task 0: Scaffold Next.js + Tailwind + shadcn + deps

**Files:**
- Create: todo el scaffold base, `package.json`, `.env.local.example`, `vitest.config.ts`

- [ ] **Step 1: Crear app Next.js**

```bash
cd ~/robi-app
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --import-alias "@/*" --use-npm --eslint --turbopack
```
(Si pregunta por sobrescribir archivos del repo vacío, aceptar; preservar `docs/` y `.git/`.)

- [ ] **Step 2: Instalar dependencias**

```bash
npm i @supabase/supabase-js @supabase/ssr @anthropic-ai/sdk framer-motion
npm i -D vitest @vitejs/plugin-react jsdom @testing-library/react
npx shadcn@latest init -d
npx shadcn@latest add button card input dialog progress avatar badge toast
```

- [ ] **Step 3: Crear `vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: { environment: 'jsdom', globals: true },
  resolve: { alias: { '@': path.resolve(__dirname, '.') } },
})
```

Agregar a `package.json` scripts: `"test": "vitest run"`, `"test:watch": "vitest"`.

- [ ] **Step 4: Crear `.env.local.example`**

```bash
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
SUPADATA_API_KEY=
```
Copiar a `.env.local` y completar con los valores reales (Supabase project + keys).

- [ ] **Step 5: Verificar que arranca**

Run: `npm run dev` → abrir http://localhost:3000 → Expected: página default de Next.js carga sin errores.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "chore: scaffold Next.js + Tailwind + shadcn + deps"
```

---

## Phase 1 — Base de datos (Supabase)

> Aplicar migraciones vía Supabase MCP (`apply_migration`) o `supabase db push`. Crear un proyecto Supabase nuevo para Robi y poner sus credenciales en `.env.local`.

### Task 1: Schema, RLS, RPC de puntos y seed de vouchers

**Files:**
- Create: `supabase/migrations/0001_schema.sql`, `0002_rls.sql`, `0003_rpc.sql`, `0004_seed_vouchers.sql`

- [ ] **Step 1: `0001_schema.sql`**

```sql
create table child_profiles (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  name text not null,
  avatar text not null,
  age int,
  total_points int not null default 0,
  created_at timestamptz not null default now()
);

create table videos (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  youtube_url text not null,
  youtube_id text not null,
  title text,
  status text not null default 'processing' check (status in ('processing','ready','rejected')),
  reject_reason text,
  transcript text,
  created_at timestamptz not null default now(),
  unique (user_id, youtube_id)
);

create table video_assignments (
  id uuid primary key default gen_random_uuid(),
  video_id uuid not null references videos(id) on delete cascade,
  child_profile_id uuid not null references child_profiles(id) on delete cascade,
  assigned_at timestamptz not null default now(),
  unique (video_id, child_profile_id)
);

create table quiz_questions (
  id uuid primary key default gen_random_uuid(),
  video_id uuid not null references videos(id) on delete cascade,
  question_text text not null,
  options jsonb not null,
  correct_index int not null check (correct_index between 0 and 3),
  source_quote text not null,
  position int not null default 0
);

create table activities (
  id uuid primary key default gen_random_uuid(),
  child_profile_id uuid not null references child_profiles(id) on delete cascade,
  video_id uuid not null references videos(id) on delete cascade,
  base_points int not null,
  bonus_points int not null,
  answers jsonb not null,
  completed_at timestamptz not null default now()
);

create table badges (
  id uuid primary key default gen_random_uuid(),
  child_profile_id uuid not null references child_profiles(id) on delete cascade,
  video_id uuid not null references videos(id) on delete cascade,
  earned_at timestamptz not null default now(),
  unique (child_profile_id, video_id)
);

create table vouchers (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  title text not null,
  description text,
  points_cost int not null,
  is_active boolean not null default true
);

create table redemptions (
  id uuid primary key default gen_random_uuid(),
  child_profile_id uuid not null references child_profiles(id) on delete cascade,
  voucher_id uuid not null references vouchers(id) on delete cascade,
  redeemed_at timestamptz not null default now()
);

create table parent_settings (
  user_id uuid primary key references auth.users(id) on delete cascade,
  pin_hash text
);
```

- [ ] **Step 2: `0002_rls.sql`** — habilitar RLS y políticas

```sql
alter table child_profiles enable row level security;
alter table videos enable row level security;
alter table video_assignments enable row level security;
alter table quiz_questions enable row level security;
alter table activities enable row level security;
alter table badges enable row level security;
alter table vouchers enable row level security;
alter table redemptions enable row level security;
alter table parent_settings enable row level security;

-- Tablas con user_id directo
create policy own_profiles on child_profiles using (user_id = auth.uid()) with check (user_id = auth.uid());
create policy own_videos on videos using (user_id = auth.uid()) with check (user_id = auth.uid());
create policy own_vouchers on vouchers using (user_id = auth.uid()) with check (user_id = auth.uid());
create policy own_settings on parent_settings using (user_id = auth.uid()) with check (user_id = auth.uid());

-- Tablas que joinean a child_profiles del usuario
create policy own_assignments on video_assignments using (
  child_profile_id in (select id from child_profiles where user_id = auth.uid())
) with check (
  child_profile_id in (select id from child_profiles where user_id = auth.uid())
);
create policy own_activities on activities using (
  child_profile_id in (select id from child_profiles where user_id = auth.uid())
) with check (
  child_profile_id in (select id from child_profiles where user_id = auth.uid())
);
create policy own_badges on badges using (
  child_profile_id in (select id from child_profiles where user_id = auth.uid())
) with check (
  child_profile_id in (select id from child_profiles where user_id = auth.uid())
);
create policy own_redemptions on redemptions using (
  child_profile_id in (select id from child_profiles where user_id = auth.uid())
) with check (
  child_profile_id in (select id from child_profiles where user_id = auth.uid())
);

-- quiz_questions: legible si el video pertenece al usuario
create policy own_questions on quiz_questions using (
  video_id in (select id from videos where user_id = auth.uid())
) with check (
  video_id in (select id from videos where user_id = auth.uid())
);
```

- [ ] **Step 3: `0003_rpc.sql`** — completar actividad de forma atómica

```sql
create or replace function complete_activity(
  p_child uuid, p_video uuid, p_correct int, p_answers jsonb
) returns table(base_points int, bonus_points int, total_points int)
language plpgsql security invoker as $$
declare v_base int := 10; v_bonus int; v_total int;
begin
  if not exists (select 1 from child_profiles where id = p_child and user_id = auth.uid()) then
    raise exception 'not authorized';
  end if;
  v_bonus := p_correct * 5;
  insert into activities(child_profile_id, video_id, base_points, bonus_points, answers)
    values (p_child, p_video, v_base, v_bonus, p_answers);
  insert into badges(child_profile_id, video_id) values (p_child, p_video)
    on conflict (child_profile_id, video_id) do nothing;
  update child_profiles set total_points = total_points + v_base + v_bonus
    where id = p_child returning total_points into v_total;
  return query select v_base, v_bonus, v_total;
end; $$;
```

- [ ] **Step 4: `0004_seed_vouchers.sql`** — se inserta por usuario en el signup (ver Task 8). Documentar aquí el catálogo base como referencia:

```sql
-- Catálogo base (se inserta por usuario en signup, no global):
-- '30 min más de pantalla' (50), 'Elegir la cena' (80), 'Noche de pijamas en el living' (120),
-- 'Una hora de juego con mamá/papá' (150), 'Elegir la película del viernes' (100),
-- 'Traer un amigo a dormir' (200)
```

- [ ] **Step 5: Aplicar migraciones** vía MCP `apply_migration` (una por archivo) o `supabase db push`. Verificar con `list_tables` que las 9 tablas existen con RLS.

- [ ] **Step 6: Generar tipos TypeScript**

```bash
npx supabase gen types typescript --project-id <PROJECT_ID> > lib/supabase/types.ts
```

- [ ] **Step 7: Commit**

```bash
git add supabase/ lib/supabase/types.ts && git commit -m "feat: schema, RLS, RPC de puntos y tipos"
```

---

## Phase 2 — Lógica core (TDD)

### Task 2: Parseo de URL de YouTube

**Files:**
- Create: `lib/youtube/parse-url.ts`
- Test: `tests/youtube-parse-url.test.ts`

**Interfaces:**
- Produces: `parseYoutubeId(url: string): string | null`

- [ ] **Step 1: Test que falla**

```ts
import { describe, it, expect } from 'vitest'
import { parseYoutubeId } from '@/lib/youtube/parse-url'

describe('parseYoutubeId', () => {
  it('extrae id de watch?v=', () => {
    expect(parseYoutubeId('https://www.youtube.com/watch?v=dQw4w9WgXcQ')).toBe('dQw4w9WgXcQ')
  })
  it('extrae id de youtu.be', () => {
    expect(parseYoutubeId('https://youtu.be/dQw4w9WgXcQ')).toBe('dQw4w9WgXcQ')
  })
  it('extrae id de /shorts/', () => {
    expect(parseYoutubeId('https://www.youtube.com/shorts/dQw4w9WgXcQ')).toBe('dQw4w9WgXcQ')
  })
  it('devuelve null para URL no-YouTube', () => {
    expect(parseYoutubeId('https://vimeo.com/123')).toBeNull()
  })
})
```

- [ ] **Step 2: Correr y ver fallar** — `npm test -- youtube-parse-url` → FAIL (módulo no existe).

- [ ] **Step 3: Implementar**

```ts
export function parseYoutubeId(url: string): string | null {
  try {
    const u = new URL(url)
    const host = u.hostname.replace('www.', '')
    if (host === 'youtu.be') return u.pathname.slice(1).split('/')[0] || null
    if (host === 'youtube.com' || host === 'm.youtube.com') {
      if (u.pathname === '/watch') return u.searchParams.get('v')
      const m = u.pathname.match(/^\/(shorts|embed)\/([^/]+)/)
      if (m) return m[2]
    }
    return null
  } catch {
    return null
  }
}
```

- [ ] **Step 4: Correr y ver pasar** — `npm test -- youtube-parse-url` → PASS.

- [ ] **Step 5: Commit** — `git add lib/youtube tests && git commit -m "feat: parseo de URL de YouTube"`

---

### Task 3: Interfaz AIProvider + validación de anclaje

**Files:**
- Create: `lib/ai/types.ts`, `lib/ai/anchoring.ts`
- Test: `tests/ai-anchoring.test.ts`

**Interfaces:**
- Produces:
  - `type QuizQuestion = { question_text: string; options: string[]; correct_index: number; source_quote: string }`
  - `interface AIProvider { filterContent(t: string): Promise<{ safe: boolean; reason?: string }>; generateQuiz(t: string): Promise<QuizQuestion[]> }`
  - `validateAnchoring(questions: QuizQuestion[], transcript: string): QuizQuestion[]` — devuelve solo las preguntas cuyo `source_quote` (normalizado) aparece en el transcript.

- [ ] **Step 1: `lib/ai/types.ts`**

```ts
export type QuizQuestion = {
  question_text: string
  options: string[]
  correct_index: number
  source_quote: string
}

export interface AIProvider {
  filterContent(transcript: string): Promise<{ safe: boolean; reason?: string }>
  generateQuiz(transcript: string): Promise<QuizQuestion[]>
}
```

- [ ] **Step 2: Test de anclaje que falla**

```ts
import { describe, it, expect } from 'vitest'
import { validateAnchoring } from '@/lib/ai/anchoring'

const t = 'El sol es una estrella. Los planetas giran alrededor del sol.'

describe('validateAnchoring', () => {
  it('mantiene preguntas con quote presente', () => {
    const q = [{ question_text: 'q', options: ['a','b','c','d'], correct_index: 0, source_quote: 'El sol es una estrella' }]
    expect(validateAnchoring(q, t)).toHaveLength(1)
  })
  it('descarta preguntas con quote inventado', () => {
    const q = [{ question_text: 'q', options: ['a','b','c','d'], correct_index: 0, source_quote: 'La luna es de queso' }]
    expect(validateAnchoring(q, t)).toHaveLength(0)
  })
  it('normaliza mayúsculas y espacios', () => {
    const q = [{ question_text: 'q', options: ['a','b','c','d'], correct_index: 0, source_quote: '  los  PLANETAS giran ' }]
    expect(validateAnchoring(q, t)).toHaveLength(1)
  })
})
```

- [ ] **Step 3: Correr y ver fallar** — `npm test -- ai-anchoring` → FAIL.

- [ ] **Step 4: Implementar `lib/ai/anchoring.ts`**

```ts
import type { QuizQuestion } from './types'

const norm = (s: string) => s.toLowerCase().replace(/\s+/g, ' ').trim()

export function validateAnchoring(questions: QuizQuestion[], transcript: string): QuizQuestion[] {
  const haystack = norm(transcript)
  return questions.filter((q) => q.source_quote && haystack.includes(norm(q.source_quote)))
}
```

- [ ] **Step 5: Correr y ver pasar** — `npm test -- ai-anchoring` → PASS.

- [ ] **Step 6: Commit** — `git add lib/ai tests && git commit -m "feat: interfaz AIProvider + validación de anclaje"`

---

### Task 4: ClaudeProvider + prompts + wiring

**Files:**
- Create: `lib/ai/prompts.ts`, `lib/ai/claude-provider.ts`, `lib/ai/index.ts`
- Test: `tests/claude-provider.test.ts` (parseo de respuesta, sin llamar a la API real)

**Interfaces:**
- Consumes: `AIProvider`, `QuizQuestion` (Task 3)
- Produces: `getAIProvider(): AIProvider` (singleton), `parseQuizResponse(text: string): QuizQuestion[]` (exportada para test)

- [ ] **Step 1: `lib/ai/prompts.ts`**

```ts
export const FILTER_SYSTEM = `Sos un filtro de seguridad infantil. Recibís la transcripción de un video. Respondé SOLO con JSON: {"safe": boolean, "reason": string}. safe=false si hay violencia explícita, lenguaje sexual, odio, o lenguaje adulto inapropiado para niños de 8-10 años.`

export const QUIZ_SYSTEM = `Sos un generador de quizzes educativos para niños de 8-10 años. A partir de la transcripción, generá EXACTAMENTE 5 preguntas de opción múltiple de comprensión. Cada pregunta: 4 opciones, una sola correcta, lenguaje simple y amable. "source_quote" debe ser un fragmento LITERAL y textual de la transcripción que justifique la respuesta. Respondé SOLO con JSON válido: {"questions":[{"question_text":string,"options":[string,string,string,string],"correct_index":0-3,"source_quote":string}]}`
```

- [ ] **Step 2: Test de parseo que falla**

```ts
import { describe, it, expect } from 'vitest'
import { parseQuizResponse } from '@/lib/ai/claude-provider'

describe('parseQuizResponse', () => {
  it('parsea JSON envuelto en texto/markdown', () => {
    const raw = '```json\n{"questions":[{"question_text":"q","options":["a","b","c","d"],"correct_index":1,"source_quote":"x"}]}\n```'
    const out = parseQuizResponse(raw)
    expect(out).toHaveLength(1)
    expect(out[0].correct_index).toBe(1)
  })
})
```

- [ ] **Step 3: Correr y ver fallar** — `npm test -- claude-provider` → FAIL.

- [ ] **Step 4: Implementar `lib/ai/claude-provider.ts`**

```ts
import Anthropic from '@anthropic-ai/sdk'
import type { AIProvider, QuizQuestion } from './types'
import { FILTER_SYSTEM, QUIZ_SYSTEM } from './prompts'

const MODEL = 'claude-sonnet-4-6'

function extractJson(text: string): any {
  const fenced = text.match(/```(?:json)?\s*([\s\S]*?)```/)
  const body = fenced ? fenced[1] : text
  const start = body.indexOf('{')
  const end = body.lastIndexOf('}')
  return JSON.parse(body.slice(start, end + 1))
}

export function parseQuizResponse(text: string): QuizQuestion[] {
  const data = extractJson(text)
  return (data.questions ?? []) as QuizQuestion[]
}

export class ClaudeProvider implements AIProvider {
  private client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })

  async filterContent(transcript: string) {
    const res = await this.client.messages.create({
      model: MODEL, max_tokens: 256, system: FILTER_SYSTEM,
      messages: [{ role: 'user', content: transcript.slice(0, 50000) }],
    })
    const text = res.content.map((b) => (b.type === 'text' ? b.text : '')).join('')
    return extractJson(text) as { safe: boolean; reason?: string }
  }

  async generateQuiz(transcript: string) {
    const res = await this.client.messages.create({
      model: MODEL, max_tokens: 2048, system: QUIZ_SYSTEM,
      messages: [{ role: 'user', content: transcript.slice(0, 50000) }],
    })
    const text = res.content.map((b) => (b.type === 'text' ? b.text : '')).join('')
    return parseQuizResponse(text)
  }
}
```

- [ ] **Step 5: `lib/ai/index.ts`** (punto único de wiring — cambiar de IA = cambiar acá)

```ts
import type { AIProvider } from './types'
import { ClaudeProvider } from './claude-provider'

let instance: AIProvider | null = null
export function getAIProvider(): AIProvider {
  if (!instance) instance = new ClaudeProvider()
  return instance
}
export type { AIProvider, QuizQuestion } from './types'
```

- [ ] **Step 6: Correr y ver pasar** — `npm test -- claude-provider` → PASS.

- [ ] **Step 7: Commit** — `git add lib/ai tests && git commit -m "feat: ClaudeProvider + prompts + wiring intercambiable"`

---

### Task 5: Scoring

**Files:**
- Create: `lib/scoring.ts`
- Test: `tests/scoring.test.ts`

**Interfaces:**
- Produces: `scoreQuiz(correctCount: number): { base: number; bonus: number; total: number }` (base=10 siempre, bonus=correct*5)

- [ ] **Step 1: Test que falla**

```ts
import { describe, it, expect } from 'vitest'
import { scoreQuiz } from '@/lib/scoring'

describe('scoreQuiz', () => {
  it('0 aciertos → 10 base, 0 bonus, 10 total', () => {
    expect(scoreQuiz(0)).toEqual({ base: 10, bonus: 0, total: 10 })
  })
  it('5 aciertos → 10 base, 25 bonus, 35 total', () => {
    expect(scoreQuiz(5)).toEqual({ base: 10, bonus: 25, total: 35 })
  })
  it('clampa entradas inválidas a 0..5', () => {
    expect(scoreQuiz(9).total).toBe(35)
    expect(scoreQuiz(-2).total).toBe(10)
  })
})
```

- [ ] **Step 2: Correr y ver fallar** — `npm test -- scoring` → FAIL.

- [ ] **Step 3: Implementar `lib/scoring.ts`**

```ts
export function scoreQuiz(correctCount: number) {
  const correct = Math.max(0, Math.min(5, Math.floor(correctCount || 0)))
  const base = 10
  const bonus = correct * 5
  return { base, bonus, total: base + bonus }
}
```

- [ ] **Step 4: Correr y ver pasar** — `npm test -- scoring` → PASS.

- [ ] **Step 5: Commit** — `git add lib/scoring.ts tests && git commit -m "feat: lógica de scoring"`

---

## Phase 3 — Supabase clients + pipeline + transcript

### Task 6: Clientes Supabase (server/client/middleware)

**Files:**
- Create: `lib/supabase/server.ts`, `lib/supabase/client.ts`, `middleware.ts`

- [ ] **Step 1: `lib/supabase/client.ts`** (browser)

```ts
import { createBrowserClient } from '@supabase/ssr'
export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

- [ ] **Step 2: `lib/supabase/server.ts`** (server con cookies)

```ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (toSet) => toSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options)),
      },
    }
  )
}
```

- [ ] **Step 3: `middleware.ts`** — refresh de sesión y protección de rutas

```ts
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(req: NextRequest) {
  let res = NextResponse.next({ request: req })
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: {
      getAll: () => req.cookies.getAll(),
      setAll: (toSet) => { toSet.forEach(({ name, value }) => req.cookies.set(name, value)); res = NextResponse.next({ request: req }); toSet.forEach(({ name, value, options }) => res.cookies.set(name, value, options)) },
    } }
  )
  const { data: { user } } = await supabase.auth.getUser()
  const path = req.nextUrl.pathname
  const isAuthPage = path.startsWith('/login') || path.startsWith('/signup')
  if (!user && !isAuthPage) return NextResponse.redirect(new URL('/login', req.url))
  if (user && isAuthPage) return NextResponse.redirect(new URL('/', req.url))
  return res
}

export const config = { matcher: ['/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg)).*)'] }
```

- [ ] **Step 4: Verificar build** — `npm run build` → Expected: compila sin errores de tipos.

- [ ] **Step 5: Commit** — `git add lib/supabase middleware.ts && git commit -m "feat: clientes Supabase + middleware de sesión"`

---

### Task 7: Transcripción (Supadata) + pipeline de video

**Files:**
- Create: `lib/youtube/transcript.ts`, `actions/videos.ts`

**Interfaces:**
- Consumes: `parseYoutubeId` (Task 2), `getAIProvider` (Task 4), `validateAnchoring` (Task 3), cliente server (Task 6)
- Produces:
  - `fetchTranscript(youtubeId: string): Promise<string | null>`
  - `processVideo(input: { url: string; childProfileId: string }): Promise<{ ok: boolean; videoId?: string; reason?: string }>` (Server Action)

- [ ] **Step 1: `lib/youtube/transcript.ts`**

```ts
export async function fetchTranscript(youtubeId: string): Promise<string | null> {
  const res = await fetch(
    `https://api.supadata.ai/v1/youtube/transcript?videoId=${youtubeId}&text=true`,
    { headers: { 'x-api-key': process.env.SUPADATA_API_KEY! } }
  )
  if (!res.ok) return null
  const data = await res.json()
  const content = typeof data.content === 'string' ? data.content : ''
  return content.trim().length > 0 ? content : null
}
```
*(Nota: validar el shape exacto de la respuesta de Supadata contra su doc al implementar; ajustar `data.content` si difiere.)*

- [ ] **Step 2: `actions/videos.ts`** — Server Action del pipeline (§4 del spec)

```ts
'use server'
import { createClient } from '@/lib/supabase/server'
import { parseYoutubeId } from '@/lib/youtube/parse-url'
import { fetchTranscript } from '@/lib/youtube/transcript'
import { getAIProvider } from '@/lib/ai'
import { validateAnchoring } from '@/lib/ai/anchoring'

const FREE_VIDEO_LIMIT = 5

export async function processVideo(input: { url: string; childProfileId: string }) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { ok: false, reason: 'No autenticado' }

  const youtubeId = parseYoutubeId(input.url)
  if (!youtubeId) return { ok: false, reason: 'La URL no es de YouTube' }

  // límite free tier por perfil
  const { count } = await supabase.from('video_assignments').select('*', { count: 'exact', head: true })
    .eq('child_profile_id', input.childProfileId)
  if ((count ?? 0) >= FREE_VIDEO_LIMIT) return { ok: false, reason: 'Llegaste al límite de 5 videos' }

  // de-dupe: reusar video ya procesado
  const { data: existing } = await supabase.from('videos').select('id,status')
    .eq('user_id', user.id).eq('youtube_id', youtubeId).maybeSingle()
  if (existing?.status === 'ready') {
    await supabase.from('video_assignments').insert({ video_id: existing.id, child_profile_id: input.childProfileId })
    return { ok: true, videoId: existing.id }
  }

  const { data: video } = await supabase.from('videos')
    .insert({ user_id: user.id, youtube_url: input.url, youtube_id: youtubeId, status: 'processing' })
    .select('id').single()
  const videoId = video!.id

  const reject = async (reason: string) => {
    await supabase.from('videos').update({ status: 'rejected', reject_reason: reason }).eq('id', videoId)
    return { ok: false, reason }
  }

  const transcript = await fetchTranscript(youtubeId)
  if (!transcript) return reject('Este video no tiene subtítulos disponibles. Probá con otro.')

  const ai = getAIProvider()
  const filter = await ai.filterContent(transcript)
  if (!filter.safe) return reject('El contenido del video no es apto. Probá con otro.')

  let questions = validateAnchoring(await ai.generateQuiz(transcript), transcript)
  if (questions.length < 5) {
    questions = validateAnchoring(await ai.generateQuiz(transcript), transcript) // reintento único
  }
  if (questions.length < 5) return reject('No pudimos generar un buen quiz para este video. Probá con otro.')

  await supabase.from('videos').update({ status: 'ready', transcript }).eq('id', videoId)
  await supabase.from('quiz_questions').insert(
    questions.slice(0, 5).map((q, i) => ({
      video_id: videoId, question_text: q.question_text, options: q.options,
      correct_index: q.correct_index, source_quote: q.source_quote, position: i,
    }))
  )
  await supabase.from('video_assignments').insert({ video_id: videoId, child_profile_id: input.childProfileId })
  return { ok: true, videoId }
}
```

- [ ] **Step 3: Verificar build** — `npm run build` → Expected: compila.

- [ ] **Step 4: Smoke test manual** — temporalmente invocar `processVideo` desde una ruta de prueba o test de integración con una URL educativa real con subtítulos. Verificar que crea `video` con `status=ready` y 5 `quiz_questions`. Borrar la ruta de prueba luego.

- [ ] **Step 5: Commit** — `git add lib/youtube actions && git commit -m "feat: transcripción Supadata + pipeline de procesamiento de video"`

---

## Phase 4 — Auth + onboarding

### Task 8: Signup / login + seed de vouchers por usuario

**Files:**
- Create: `app/(auth)/login/page.tsx`, `app/(auth)/signup/page.tsx`, `actions/auth.ts`

**Interfaces:**
- Consumes: clientes Supabase
- Produces: `signUp(email, password)`, `signIn(email, password)`, `signOut()` server actions. `signUp` además inserta el catálogo de vouchers base para ese usuario.

- [ ] **Step 1: `actions/auth.ts`**

```ts
'use server'
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'

const SEED_VOUCHERS = [
  { title: '30 min más de pantalla', description: 'Tiempo extra hoy', points_cost: 50 },
  { title: 'Elegir la cena', description: 'Vos elegís el menú', points_cost: 80 },
  { title: 'Elegir la película del viernes', description: 'Noche de cine familiar', points_cost: 100 },
  { title: 'Noche de pijamas en el living', description: 'Campamento indoor', points_cost: 120 },
  { title: 'Una hora de juego con mamá/papá', description: 'Tiempo exclusivo', points_cost: 150 },
  { title: 'Traer un amigo a dormir', description: 'Pijamada', points_cost: 200 },
]

export async function signUp(email: string, password: string) {
  const supabase = await createClient()
  const { data, error } = await supabase.auth.signUp({ email, password })
  if (error) return { error: error.message }
  if (data.user) {
    await supabase.from('vouchers').insert(SEED_VOUCHERS.map((v) => ({ ...v, user_id: data.user!.id })))
  }
  redirect('/onboarding')
}

export async function signIn(email: string, password: string) {
  const supabase = await createClient()
  const { error } = await supabase.auth.signInWithPassword({ email, password })
  if (error) return { error: error.message }
  redirect('/')
}

export async function signOut() {
  const supabase = await createClient()
  await supabase.auth.signOut()
  redirect('/login')
}
```

- [ ] **Step 2: `app/(auth)/signup/page.tsx` y `login/page.tsx`** — formularios client component con shadcn `Card`/`Input`/`Button`, estética playful, logo de Robi. El submit llama a la server action correspondiente y muestra el `error` si vuelve. (Signup pide email+password; login igual + link a signup.)

- [ ] **Step 3: Verificar manual** — registrarse con un email de prueba → redirige a `/onboarding`; verificar que se crearon 6 vouchers para el usuario en Supabase.

- [ ] **Step 4: Commit** — `git add app actions/auth.ts && git commit -m "feat: signup/login + seed de vouchers"`

---

### Task 9: Onboarding + creación de perfil

**Files:**
- Create: `app/onboarding/page.tsx`, `actions/profiles.ts`

**Interfaces:**
- Produces: `createProfile(input: { name: string; avatar: string; age?: number }): Promise<{ ok: boolean; profileId?: string; reason?: string }>` — enforce límite de 1 perfil free tier.

- [ ] **Step 1: `actions/profiles.ts`**

```ts
'use server'
import { createClient } from '@/lib/supabase/server'

const FREE_PROFILE_LIMIT = 1

export async function createProfile(input: { name: string; avatar: string; age?: number }) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { ok: false, reason: 'No autenticado' }
  const { count } = await supabase.from('child_profiles').select('*', { count: 'exact', head: true })
  if ((count ?? 0) >= FREE_PROFILE_LIMIT) return { ok: false, reason: 'La versión gratuita permite 1 perfil. Pasá a la versión paga para agregar más.' }
  const { data, error } = await supabase.from('child_profiles')
    .insert({ user_id: user.id, name: input.name, avatar: input.avatar, age: input.age }).select('id').single()
  if (error) return { ok: false, reason: error.message }
  return { ok: true, profileId: data.id }
}
```

- [ ] **Step 2: `app/onboarding/page.tsx`** — paso obligatorio: form nombre + selector de avatar (grilla de emojis/ilustraciones). Al crear, sugiere cargar el primer video con botón "Saltear por ahora" → `/`. Si ya hay un perfil, redirige a `/`.

- [ ] **Step 3: Verificar manual** — crear perfil → aparece opción de cargar video → saltear lleva a la home.

- [ ] **Step 4: Commit** — `git add app/onboarding actions/profiles.ts && git commit -m "feat: onboarding + creación de perfil con límite free tier"`

---

## Phase 5 — Home (selección de perfiles) + PIN

### Task 10: Home de selección de perfiles + gate de PIN

**Files:**
- Create: `app/page.tsx`, `actions/pin.ts`, `app/parent/layout.tsx`
- Modify: usa `components/robi/Robi.tsx` (creado en Task 15; hasta entonces placeholder emoji 🤖)

**Interfaces:**
- Consumes: `createClient` (server)
- Produces: `setPin(pin: string)`, `verifyPin(pin: string): Promise<{ ok: boolean }>` (hash con SHA-256 vía `crypto`). El gate del panel adulto se maneja con una cookie de sesión corta `parent_unlocked`.

- [ ] **Step 1: `actions/pin.ts`**

```ts
'use server'
import { createClient } from '@/lib/supabase/server'
import { cookies } from 'next/headers'
import { createHash } from 'crypto'

const hash = (pin: string) => createHash('sha256').update(pin).digest('hex')

export async function setPin(pin: string) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { ok: false }
  await supabase.from('parent_settings').upsert({ user_id: user.id, pin_hash: hash(pin) })
  return { ok: true }
}

export async function verifyPin(pin: string) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { ok: false }
  const { data } = await supabase.from('parent_settings').select('pin_hash').eq('user_id', user.id).maybeSingle()
  // primer uso: si no hay PIN configurado, este lo establece
  if (!data?.pin_hash) { await setPin(pin); (await cookies()).set('parent_unlocked', '1', { maxAge: 1800 }); return { ok: true } }
  if (data.pin_hash === hash(pin)) { (await cookies()).set('parent_unlocked', '1', { maxAge: 1800 }); return { ok: true } }
  return { ok: false }
}
```

- [ ] **Step 2: `app/page.tsx`** (server component) — lee perfiles del usuario; renderiza tarjetas (avatar + nombre + `total_points`) que linkean a `/kid/[id]`. Botón "Panel de adultos 🔒" que abre un `Dialog` de PIN. Botón "+ Agregar perfil" (muestra CTA de versión paga si ya hay 1). Si no hay perfiles, redirige a `/onboarding`.

- [ ] **Step 3: `app/parent/layout.tsx`** — lee la cookie `parent_unlocked`; si no está, redirige a `/` (donde se ingresa el PIN). El `Dialog` de PIN en la home llama a `verifyPin` y, si `ok`, navega a `/parent`.

- [ ] **Step 4: Verificar manual** — home muestra la tarjeta del perfil; PIN abre el panel; PIN incorrecto lo bloquea.

- [ ] **Step 5: Commit** — `git add app/page.tsx app/parent/layout.tsx actions/pin.ts && git commit -m "feat: home de perfiles + gate de PIN"`

---

## Phase 6 — Panel del adulto

### Task 11: Cargar video (con loader de Robi) + dashboard

**Files:**
- Create: `app/parent/page.tsx`, `app/parent/add-video/page.tsx`

**Interfaces:**
- Consumes: `processVideo` (Task 7), perfiles

- [ ] **Step 1: `app/parent/add-video/page.tsx`** (client) — selector de perfil, input de URL, **checkbox obligatorio** "Revisé el video y es apto para mi hijo/a" (deshabilita submit hasta tildar). Al enviar, llama a `processVideo` y muestra estado de carga con Robi animado (mood "thinking"). Si vuelve `{ ok:false }`, muestra `reason` con tono amable y deja reintentar. Si `ok`, muestra preview opcional de las preguntas generadas (fetch a `quiz_questions` por `videoId`) — no bloqueante — y botón "Listo".

- [ ] **Step 2: `app/parent/page.tsx`** (server) — dashboard: por cada perfil, lista videos asignados con su estado (processing/ready/rejected/visto), cantidad de actividades completadas y `total_points`. Links a "Cargar video" y "Gestionar vales".

- [ ] **Step 3: Verificar manual** — cargar una URL educativa real → ver loader de Robi → video queda `ready` con 5 preguntas → aparece en el dashboard.

- [ ] **Step 4: Commit** — `git add app/parent && git commit -m "feat: carga de video con loader de Robi + dashboard"`

---

### Task 12: Gestión de vales (vouchers)

**Files:**
- Create: `app/parent/vouchers/page.tsx`, agregar a `actions/profiles.ts` o nuevo `actions/vouchers.ts`: `toggleVoucher(id, isActive)`

- [ ] **Step 1: `actions/vouchers.ts`**

```ts
'use server'
import { createClient } from '@/lib/supabase/server'
export async function toggleVoucher(id: string, isActive: boolean) {
  const supabase = await createClient()
  await supabase.from('vouchers').update({ is_active: isActive }).eq('id', id)
  return { ok: true }
}
```

- [ ] **Step 2: `app/parent/vouchers/page.tsx`** — lista los vouchers del usuario con toggle activo/inactivo (los activos aparecen en el catálogo del niño). Editar costo/título es opcional (nice-to-have).

- [ ] **Step 3: Verificar manual** — desactivar un vale → no aparece en `/kid/[id]/rewards`.

- [ ] **Step 4: Commit** — `git add app/parent/vouchers actions/vouchers.ts && git commit -m "feat: gestión de vales del adulto"`

---

## Phase 7 — Mundo del niño

### Task 13: Biblioteca + reproductor

**Files:**
- Create: `app/kid/[profileId]/page.tsx`, `app/kid/[profileId]/watch/[videoId]/page.tsx`

**Interfaces:**
- Consumes: `child_profiles`, `video_assignments` join `videos`, `activities` (para estado visto/no visto)

- [ ] **Step 1: `app/kid/[profileId]/page.tsx`** (server) — header con avatar, nombre y puntos del niño; accesos a "Mi álbum" y "Premios". Grilla de videos asignados (status `ready`) con badge visual "✓ Visto" si existe una `activity` para ese (perfil, video), o "Nuevo" si no. Tap → `/watch/[videoId]`.

- [ ] **Step 2: `watch/[videoId]/page.tsx`** (client) — embebe el YouTube IFrame Player API. Suscribe al evento `onStateChange`; cuando `data === YT.PlayerState.ENDED`, muestra el botón grande "¡Listo! Hacer la actividad" que navega a `/kid/[profileId]/quiz/[videoId]`. (También mostrar el botón si el niño llega al final; no autodetección de quiz.)

- [ ] **Step 3: Verificar manual** — el niño abre su biblioteca, reproduce un video, al terminar aparece el botón.

- [ ] **Step 4: Commit** — `git add app/kid && git commit -m "feat: biblioteca del niño + reproductor con botón al finalizar"`

---

### Task 14: Quiz con Robi + resultado

**Files:**
- Create: `app/kid/[profileId]/quiz/[videoId]/page.tsx`, `app/kid/[profileId]/result/page.tsx`, `actions/quiz.ts`

**Interfaces:**
- Consumes: `scoreQuiz` (Task 5), RPC `complete_activity` (Task 1)
- Produces: `submitQuiz(input: { childProfileId, videoId, answers: number[] }): Promise<{ base, bonus, total, correctCount }>`

- [ ] **Step 1: `actions/quiz.ts`**

```ts
'use server'
import { createClient } from '@/lib/supabase/server'

export async function submitQuiz(input: { childProfileId: string; videoId: string; answers: number[] }) {
  const supabase = await createClient()
  const { data: questions } = await supabase.from('quiz_questions')
    .select('correct_index,position').eq('video_id', input.videoId).order('position')
  let correct = 0
  ;(questions ?? []).forEach((q, i) => { if (input.answers[i] === q.correct_index) correct++ })
  const { data } = await supabase.rpc('complete_activity', {
    p_child: input.childProfileId, p_video: input.videoId, p_correct: correct, p_answers: input.answers,
  }).single()
  return { base: data!.base_points, bonus: data!.bonus_points, total: data!.total_points, correctCount: correct }
}
```

- [ ] **Step 2: `quiz/[videoId]/page.tsx`** (client) — carga las 5 preguntas (fetch a `quiz_questions` por video, ordenadas por `position`). Una pregunta por pantalla con 4 opciones; **feedback inmediato** (verde/rojo) al responder; barra de progreso (`respondidas/total`); Robi guía. Toggle de TTS (Web Speech API) que lee pregunta + opciones. Al responder las 5, llama `submitQuiz` y navega a `/result` pasando los datos (via query params o estado en `sessionStorage`).

- [ ] **Step 3: `result/page.tsx`** (client) — celebración con Robi (mood "celebrate", Framer Motion), muestra: badge obtenido, puntos ganados (base + bonus), total acumulado. Botones "Volver a la biblioteca" y "Ver mi álbum".

- [ ] **Step 4: Verificar manual** — completar un quiz con N aciertos → puntos correctos según `scoreQuiz` → `total_points` del perfil actualizado → badge creado.

- [ ] **Step 5: Commit** — `git add app/kid actions/quiz.ts && git commit -m "feat: quiz con Robi + resultado + scoring atómico"`

---

### Task 15: Componente Robi (mascota)

**Files:**
- Create: `components/robi/Robi.tsx`
- Modify: reemplazar placeholders 🤖 en loader/quiz/result por `<Robi mood=... />`

**Interfaces:**
- Produces: `<Robi mood="idle" | "thinking" | "celebrate" | "talking" size?={number} />`

- [ ] **Step 1: Crear `components/robi/Robi.tsx`** — ilustración SVG de Robi (robotito) con variación por `mood` (ojos/boca/color) y animación con Framer Motion (flotar en idle, girar engranaje en thinking, saltar en celebrate). Si no se ilustra a mano, usar un SVG simple compuesto de formas + animaciones; los assets pueden refinarse después sin tocar la API del componente.

- [ ] **Step 2: Integrar** en el loader de `add-video`, el quiz y el result.

- [ ] **Step 3: Verificar manual** — Robi aparece animado en los 3 lugares.

- [ ] **Step 4: Commit** — `git add components/robi && git commit -m "feat: mascota Robi con moods animados"`

---

### Task 16: Álbum de badges + premios (canje mock)

**Files:**
- Create: `app/kid/[profileId]/album/page.tsx`, `app/kid/[profileId]/rewards/page.tsx`

- [ ] **Step 1: `album/page.tsx`** (server) — grilla tipo álbum de los `badges` del perfil (cada uno con el título/tema del video). Slots vacíos para los videos aún no completados, para dar sensación de colección.

- [ ] **Step 2: `rewards/page.tsx`** (client) — muestra `total_points` del niño y el catálogo de `vouchers` activos del padre. Cada vale: título, descripción, `points_cost`, y botón "Canjear" habilitado solo si `total_points >= points_cost`. **MOCK**: al tocar, muestra un `Dialog` de confirmación visual ("¡Pedíselo a mamá/papá!") con Robi celebrando, pero **no** escribe `redemptions` ni descuenta puntos.

- [ ] **Step 3: Verificar manual** — álbum muestra badges ganados; premios lista vales activos; canje muestra confirmación sin alterar puntos.

- [ ] **Step 4: Commit** — `git add app/kid && git commit -m "feat: álbum de badges + catálogo de premios con canje mock"`

---

## Phase 8 — Deploy y cierre

### Task 17: Deploy a Vercel + set de videos de demo

**Files:**
- Modify: `README.md` (instrucciones de env + set de URLs de demo)

- [ ] **Step 1: Push del repo a GitHub** (`NachoQu/robi-app`) y conectar el proyecto en Vercel.

- [ ] **Step 2: Cargar variables de entorno** en Vercel (las 5 de `.env.local.example`).

- [ ] **Step 3: Deploy** y verificar el flujo completo padre→niño en la URL de producción.

- [ ] **Step 4: Preparar set de demo** — 3-5 URLs de videos educativos infantiles en español **con subtítulos verificados**, probados de punta a punta. Documentarlas en el README.

- [ ] **Step 5: Commit** — `git add README.md && git commit -m "docs: instrucciones de deploy + set de videos de demo"`

---

## Notas de ejecución

- **Riesgo R3 (calidad del quiz):** después de la Task 7, validar el prompt con 3-5 videos reales antes de seguir con la UI. Si la calidad es baja, iterar `lib/ai/prompts.ts` (no la lógica).
- **Orden recomendado:** Phases 0→3 dan el motor (procesar video → quiz). Recién ahí construir UI (4→7). Phase 8 al final.
- **Tests automáticos** cubren la lógica pura (Tasks 2-5). La UI se verifica manualmente por el criterio de demo del PRD.
